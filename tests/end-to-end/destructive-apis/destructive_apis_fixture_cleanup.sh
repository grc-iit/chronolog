#!/usr/bin/env bash
# FIXTURES_CLEANUP for end-to-end destructive-API tests (#574).
# No-op: each test cleans up its own chronicle in its main flow. The
# deployment itself is owned by the outer pipeline / developer.
exit 0
