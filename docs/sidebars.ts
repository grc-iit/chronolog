import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

// Current/unversioned docs are disabled via `includeCurrentVersion: false`
// in docusaurus.config.ts; each released version supplies its own sidebar
// under docs/versioned_sidebars/. This empty config is kept only because
// the docs plugin requires a sidebarPath to resolve.
const sidebars: SidebarsConfig = {};

export default sidebars;
